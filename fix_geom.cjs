const fs = require('fs'); const data = JSON.parse(fs.readFileSync('public/Kerala_140_AC_Geo_Data.json', 'utf8')); const getCenter = (geom) => { let sumX=0, sumY=0, c=0; geom.coordinates[0][0].forEach(p => { sumX+=p[0]; sumY+=p[1]; c++; }); return [sumX/c, sumY/c]; }; for(let i=70; i<=88; i++) console.log(i, getCenter(data[i].geometry).map(n=>n.toFixed(2)).join(', ')); console.log(139, getCenter(data[139].geometry).map(n=>n.toFixed(2)).join(', '));
